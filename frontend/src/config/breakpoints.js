// Single source of truth for responsive breakpoints shared between
// tailwind.config.js and any JS that needs numeric px values (e.g. react-masonry-css).
export const screens = {
  md: 768,
  xl: 1280,
};
